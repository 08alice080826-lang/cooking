# AGENTS.md

## 最優先運用ルール
- このプロジェクトで仕様・文言・デザイン・機能の修正を行うときは、必ず最初にこの `AGENTS.md` を更新してから実装に着手すること。
- 実装後に仕様との差分が出た場合は、同一作業内で `AGENTS.md` を最新状態へ更新すること。

## 1. 概要
- サイト名: ひとつうえのお菓子作りを、
- 目的: お菓子レッスンの魅力訴求、レシピ紹介、体験レッスン申込導線の提供
- ターゲット: お菓子作りに興味のある初心者から中級者
- デザイン方針: 大人っぽくシンプル、やわらかいミルク系ピンクトーン
- タイポグラフィ: 本文は `Noto Sans JP`、見出し（h1/h2/h3）は `Noto Serif JP`

## 2. 構成
- `index.html`: 単一ページ構成（ランディングページ）
- `styles.css`: 全体スタイル、レスポンシブ対応
- `script.js`: ナビ開閉、フォーム送信メッセージ、画像未配置時フォールバック
- `images/`: Lesson画像格納フォルダ

## 3. セクション仕様
- ヘッダー
  - サイト名表示
  - アンカーリンクナビ（room / Lesson / menu / レッスン / 申込）
  - モバイル時はメニューボタンで開閉
- ヒーロー
  - 教室コンセプト文言
  - 英語サブタイトル文言は「akiko baking class」
  - 背景は3枚の写真を使用した自動スライド表示（3秒ごとに切替）
  - CTAボタン（体験レッスン予約 / レシピを見る）
- 今月のlesson（旧: 今月のテーマ）
  - `lesson` セクション内に含めて配置し、「人気のlesson」とつながる構成にする
  - セクション内に「今月のlesson」と明記
  - 「今月のlesson」表記はカード枠の外に配置
  - 「今月のlesson」の文字サイズは「人気のlesson」と同一
  - 上部に大きい「Lesson」見出しを表示し、色は `--accent`、下線（アンダーバー）を付与
  - 「Lesson」見出しのアンダーバーは左端まで伸ばす
  - 写真 + テーマ名 + 開催情報（開催日 / 時間 / 定員）を表示
  - 「体験レッスンを予約」ボタンを配置（申込セクションへ遷移）
  - 写真は利用者が任意の画像へ差し替えて利用（`images/monthly-theme.jpg`）
- About
  - 上部に「Room」見出しを「Lesson」と同じ表記スタイルで表示
  - セクションラベルは `about` ではなく「教室について」を表示
  - 写真と文章は区切らず、一体表示のレイアウト
  - 写真は右側へ向かって徐々に白背景へフェード
  - フェードして白くなる領域に文章を重ねて表示
  - 見出しは「ひとつうえのお菓子作りを、」で改行し、次行を「毎日の台所で。」とする
  - 見出し1行目「ひとつうえのお菓子作りを、」は1行表示を維持
  - 説明文ブロックは右寄せしすぎず、左へオフセットして配置
  - 写真は利用者が任意の画像へ差し替えて利用（`images/about-class.jpg`）
  - `room` / `lesson` / `menu` の背景境界は自然に切り替わるグラデーションでつなぐ
- Lesson（旧: 教室で作ったお菓子）
  - 6項目: クロワッサン / カヌレ / オレンジタルト / マカロン / エクレア / ベイクドチーズケーキ
  - 見出しは「人気のlesson」
  - 見出し下に小さめの補足文「人気のレッスンです。初心者からでも安心してご参加いただけます。興味のあるlessonにお申し込みください。」を表示
  - 各項目に画像、タイトルを表示（説明文は表示しない）
  - 各カード右下に細い線の右向き矢印マークを表示（色は `--accent`）
  - 画像だけでなく、右下矢印を含むカード押下でも詳細ポップアップを開く
  - カードは右から左へ自動で連続スクロール表示
  - 1周目と2周目の継ぎ目で途切れない無限ループ表示
  - 継ぎ目は「ベイクドチーズケーキ」の次に「クロワッサン」が自然につながること
  - 継ぎ目を含む全カード間隔は同一にする
  - 無限スクロールは余白が出ないようJSでループ制御
  - 自動スクロールを維持したまま、利用者が左右へドラッグ操作できる
  - 各レッスン画像をタップ/クリックすると詳細をポップアップ表示
  - ポップアップ内容は「写真」「料金（7000円）」「人数（6人）」「各お菓子の魅力」
  - レシピポップアップの「lesson recipe」表記は表示しない
  - レシピポップアップに「申込む」ボタンを配置し、色は「体験レッスンを予約」ボタンと同一
- Menu（旧: Recipes / 人気レシピ）
  - 上部に「Menu」見出しを「Lesson」と同じ表記スタイルで表示
  - セクション表記は「menu」
  - 商品一覧はミニマルなグリッドレイアウト
  - PC表示は3列グリッド
  - 各商品は枠線・背景色・カード影なし
  - 商品画像 / 商品名 / 説明を縦並びで表示（価格は表示しない）
  - 商品画像は大きめで中央配置
  - テキストは中央揃え
  - 余白を広めに取り、すっきりした高級感のある見た目にする
  - 背景グラデーションの始点色は `lesson`（スイーツ背景）と同色に合わせる
  - `menu` の白っぽい商品背景に合わせ、背面の土台色も同系色グラデーションで一体化させる
  - `menu` 透過画像の土台色ずれを避けるため、画像背景は透明表示にして背面色と一致させる
  - 画像は利用者が任意の写真へ差し替えて利用
  - 使用ファイル: `images/menu-1.png` / `images/menu-2.png` / `images/menu-3.png` / `images/menu-4.png` / `images/menu-5.png` / `images/menu-6.png`
  - `menu` 一覧の全商品画像（`images/menu-1.png`〜`images/menu-6.png`）は透過PNGを使用
  - 商品名対応:
    - `menu-1`: シュークリーム
    - `menu-2`: パンナコッタ
    - `menu-3`: 桜モンブラン
    - `menu-4`: クッキーサンド
    - `menu-5`: 塩パン
    - `menu-6`: わらび餅
- Lesson Schedule
  - 月間スケジュール表示
  - 講師紹介カード
- Contact
  - 電話番号、住所表示
  - 申込フォーム（名前 / メール / 希望日 / メッセージ）
- フッター
  - コピーライト
  - ページ上部リンク

## 4. Lesson画像仕様
- 配置先: `images/`
- 使用ファイル:
  - `images/lesson-croissant.jpg`
  - `images/lesson-canele.jpg`
  - `images/lesson-orange-tart.jpg`
  - `images/lesson-macaron.jpg`
  - `images/lesson-eclair.jpg`
  - `images/lesson-baked-cheesecake.jpg`
- 推奨:
  - 拡張子は `.jpg`
  - 横幅1200px以上
  - 表示は `object-fit: cover` でトリミング

## 5. 振る舞い仕様
- ハンバーガーメニュー
  - 700px以下で表示
  - ボタン押下でナビを開閉
- フォーム送信
  - 現状はフロントのみ（送信先なし）
  - 送信時に完了メッセージを表示してフォームをリセット
- 画像未配置時
  - 読み込みエラー画像を非表示にし、背景のみ表示
- ヒーロー背景スライド
  - 対象: ページ先頭のヒーローセクション
  - 3枚の背景写真を3秒間隔で自動切替（フェード）
  - 使用ファイル: `images/hero-1.jpg` / `images/hero-2.jpg` / `images/hero-3.jpg`
  - 写真は利用者が任意の画像に差し替えて利用

## 6. レスポンシブ仕様
- ブレークポイント
  - `max-width: 980px`: 2カラム構成を1カラム化
  - `max-width: 700px`: ナビをモバイル表示へ切替
- スマホで主要コンテンツが縦積みで閲覧可能

## 7. 今後の拡張候補
- 申込フォームのバックエンド連携（メール通知またはDB保存）
- Lesson画像の管理画面化
- レシピの詳細ページ分割
- OGP / favicon / SEOメタ強化

## 8. 公開運用（GitHub Pages）
- 公開先リポジトリ: `https://github.com/08alice080826-lang/cooking`
- ブランチ: `main`
- 配信元: GitHub Pages の `Deploy from a branch` を使用し、`main` ブランチの `/ (root)` を公開
- 共有URL: `https://08alice080826-lang.github.io/cooking/`
- 公開対象: ルート直下の `index.html` / `styles.css` / `script.js` / `images/`
